#include<stdio.h>
int main ()
{
int a,b,c,order;
ord=ordercheck(a,b,c);
if(ord==1)
    printf("in order a,b,c");
    else
        printf("not in order")
}int check(int a,intb,intc);
if(a<b&&b<c)
    return 1;
else
    return 0;
}

