#include<stdio.h>
int main()
{
    int n,i,count=0,num=9;
    for(i=2;i<n;i++){
        if(num%i==0){
   // printf("num is prime");}
    count++;} }
    if (count==2)
       printf("prime number");
       else
        printf("not a prime number");
        return 0;
}
