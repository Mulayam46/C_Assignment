#include<stdio.h>
int reversearray(int );
int main()
{
   int i,a[5];
   printf("Enter an arr");
   scanf("%d",&a[i]);
   reversearray(a[i]);
}
int reversearray(int a[])
{
   int i;
   for(i=5;i>=1;i--)
   return a[i];

}
